name: blog
services:
  blog_nginx:
    build:
      context: ./.docker/nginx
    container_name: blog_nginx
    ports:
      - "8080:80"
      - "443:443"
    volumes:
      - ./.docker/nginx/conf.d:/etc/nginx/conf.d
      - ./:/var/www/html
      - ./.docker/nginx/logs:/var/log/nginx
      - ./.docker/nginx/certs:/var/certs
    depends_on:
      - blog_php_8.2
    networks:
      - shared_network

  blog_php_8.2:
    build:
      context: ./.docker
    container_name: blog_php_8.2
    extra_hosts:
      - host.docker.internal:host-gateway
    volumes:
      - ./.docker/php/conf.d/xdebug.ini:/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini
      - ./:/var/www/html
    user: "ubuntu"
    networks:
      - shared_network
    depends_on:
      - blog_mariadb_10.11

  blog_mariadb_10.11:
    build:
      context: ./.docker/mysql
    container_name: blog_mariadb_10.11
    command: --default-authentication-plugin=mysql_native_password
    volumes:
      - blog_mysql_data:/var/lib/mysql
      - ./.docker/mysql/init:/docker-entrypoint-initdb.d
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-blog}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-blog}
      MYSQL_USER: ${MYSQL_USER:-blog}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-blog}
    ports:
      - "${MYSQL_PORT:-3336}:3306"
    networks:
      - shared_network

  blog_adminer:
    container_name: blog_adminer
    image: adminer
    ports:
      - "8081:8080"
    networks:
      - shared_network
    depends_on:
      - blog_mariadb_10.11

  blog_node_20:
    container_name: blog_node_20
    image: node:20-alpine
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
    command: ["sh", "-c", "tail -f /dev/null"]
    networks:
      - shared_network

networks:
  shared_network:
    name: my_shared_network

volumes:
  blog_mysql_data:
